# =============================================================================
# SuperBizAgent 环境变量模板（可提交到 GitHub）
# =============================================================================
#
# 【怎么操作】
# 1. 复制本文件为本地真实配置（任选一种命名）：
#      Windows PowerShell:  Copy-Item .env.example .env
#      Windows CMD:         copy .env.example .env
#      macOS / Linux:       cp .env.example .env
#    也可用中文副本名：Copy-Item .env.example .env-副本  （再改名为 .env 后使用）
# 2. 打开 .env，把下面 YOUR_xxx 占位符换成真实密钥（不要加引号除非值里有空格）
# 3. .env 已被 .gitignore 忽略，不会进 GitHub；本文件 .env.example 可以提交
#
# 【怎么传递给同事 / 部署环境】（禁止：钉钉/微信明文、GitHub Issue/PR、邮件明文）
# - 推荐：企业密码管理器（1Password / Bitwarden / 公司密钥中心）共享条目
# - CI/CD：在 GitHub Actions / 流水线 Secrets 里配置同名变量，再注入到运行环境
# - Docker Compose：项目根目录放好 .env 后，compose 会自动读取 ${VAR}
# - 本地 Spring Boot：
#     PowerShell:  $env:DASHSCOPE_API_KEY="sk-xxxx"
#     bash:        export DASHSCOPE_API_KEY=sk-xxxx
#     或 IDE Run Configuration → Environment variables 填入
# - 服务器：用 systemd EnvironmentFile=、K8s Secret、或云厂商密钥托管，不要把 .env 打进镜像
#
# 【校验是否误提交】
#   git status    # 不应出现 .env
#   git check-ignore -v .env
# =============================================================================

# ---------- 阿里云 DashScope（应用主密钥，必填）----------
DASHSCOPE_API_KEY=YOUR_DASHSCOPE_API_KEY

# ---------- 腾讯云 CLS MCP（仅用 vector-database+cls-mcp.yml 时需要）----------
TENCENTCLOUD_SECRET_ID=YOUR_TENCENTCLOUD_SECRET_ID
TENCENTCLOUD_SECRET_KEY=YOUR_TENCENTCLOUD_SECRET_KEY
# 内网可改为：internal.tencentcloudapi.com
TENCENTCLOUD_API_BASE_HOST=tencentcloudapi.com

# ---------- MinIO（可选；默认仅本地演示，生产请改强密码）----------
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# ---------- Docker 数据卷根目录（可选）----------
# DOCKER_VOLUME_DIRECTORY=.
